using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BlockSliderScript : MonoBehaviour
{


    public UnityEngine.UI.Slider slider;

     void Start()
    {
        slider.value = 200;    
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
