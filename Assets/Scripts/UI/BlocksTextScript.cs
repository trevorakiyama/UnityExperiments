using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BlocksTextScript : MonoBehaviour
{


    UnityEngine.UI.Text text;

    // Start is called before the first frame update
    void Start()
    {

        text.text = "HELLO";


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
